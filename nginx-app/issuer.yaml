apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-production
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: donwald.hartman@gmail.com
    privateKeySecretRef:
      name: ghost-issuer-key
    solvers:
      - http01:
          ingress:
            class: nginx
